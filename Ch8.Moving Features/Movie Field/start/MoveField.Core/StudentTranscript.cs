using System;

namespace MoveField.Core
{
    public class StudentTranscript
    {
        private DateTime _contestDate;

        public StudentTranscript(DateTime contestDate)
        {
            _contestDate = contestDate;
        }
    }
}